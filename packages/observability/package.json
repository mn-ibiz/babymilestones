{
  "name": "@bm/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Structured logging (pino) + correlation IDs, redaction, error-tracking seam, and alert rules",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "pino": "^10.3.1" },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
