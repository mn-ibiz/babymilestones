import { CommissionRatesClient } from "./commission-rates-client";

/** Admin per-staff commission-rate management page (P3-E01-S01). */
export default function CommissionRatesPage() {
  return <CommissionRatesClient />;
}
