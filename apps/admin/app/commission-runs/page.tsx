import { CommissionRunsClient } from "./commission-runs-client";

/** Admin Reports → ad-hoc commission run + run history (P3-E01-S04/S05). */
export default function CommissionRunsPage() {
  return <CommissionRunsClient />;
}
