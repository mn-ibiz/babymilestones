import { CashUp } from "../../components/CashUp";

/**
 * End-of-day cash-up page (P2-E04-S05), inside the role-gated POS shell. The
 * "End of day" CTA in the till header links here.
 */
export default function CashUpPage() {
  return <CashUp />;
}
